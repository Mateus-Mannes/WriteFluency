apiVersion: apps/v1
kind: Deployment
metadata:
  name: wf-users-api
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: wf-users-api
        envFrom:
        - configMapRef:
            name: wf-users-api-env
        - secretRef:
            name: wf-users-secrets
