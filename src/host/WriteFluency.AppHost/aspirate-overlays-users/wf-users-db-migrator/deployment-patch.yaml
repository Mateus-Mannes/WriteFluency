apiVersion: apps/v1
kind: Deployment
metadata:
  name: wf-users-db-migrator
spec:
  template:
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      initContainers:
      - name: wait-for-postgres
        image: busybox
        command: ['sh', '-c', 'until nc -z wf-postgres 5432; do echo waiting for postgres; sleep 2; done;']
      containers:
      - name: wf-users-db-migrator
        envFrom:
        - configMapRef:
            name: wf-users-db-migrator-env
        - secretRef:
            name: wf-users-secrets
