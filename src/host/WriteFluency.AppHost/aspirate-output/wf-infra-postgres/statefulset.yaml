---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: wf-infra-postgres
  labels:
    app: wf-infra-postgres
spec:
  serviceName: "wf-infra-postgres"
  replicas: 1
  selector:
    matchLabels:
      app: wf-infra-postgres
  template:
    metadata:
      labels:
        app: wf-infra-postgres
    spec:
      containers:
        - name: wf-infra-postgres
          image: docker.io/postgres:14.3
          imagePullPolicy: Always
          ports:
            - containerPort: 5432
          envFrom:
            - configMapRef:
                name: wf-infra-postgres-env
          volumeMounts:
            - name: writefluency-postgres-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: writefluency-postgres-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
