resources:
- statefulset.yaml
- service.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: wf-infra-minio-env
  literals:
    - MINIO_ROOT_USER=minioadmin
    - MINIO_ROOT_PASSWORD=admin123

