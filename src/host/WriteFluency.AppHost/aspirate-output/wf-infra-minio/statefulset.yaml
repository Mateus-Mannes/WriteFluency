---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: wf-infra-minio
  labels:
    app: wf-infra-minio
spec:
  serviceName: "wf-infra-minio"
  replicas: 1
  selector:
    matchLabels:
      app: wf-infra-minio
  template:
    metadata:
      labels:
        app: wf-infra-minio
    spec:
      containers:
        - name: wf-infra-minio
          image: docker.io/minio/minio:release.2025-06-13t11-33-47z
          imagePullPolicy: Always
          args:
            - 'server'
            - '/data'
            - '--console-address'
            - ':9001'
          ports:
            - containerPort: 9000
            - containerPort: 9001
          envFrom:
            - configMapRef:
                name: wf-infra-minio-env
          volumeMounts:
            - name: writefluency-minio-data
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: writefluency-minio-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
